# Clé API Anthropic — utilisée uniquement côté serveur (app/api/chat/route.ts).
# Obtenez-en une sur https://console.anthropic.com/
# L'assistant IA se désactive proprement si cette clé est absente ; le simulateur
# continue de fonctionner normalement.
ANTHROPIC_API_KEY=sk-ant-...
